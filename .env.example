# pybmpmon Configuration Example
# Copy this file to .env and customize for your environment
# Security: Set permissions with: chmod 600 .env

# ===================================================================
# BMP Listener Configuration
# ===================================================================

# Host to bind to (0.0.0.0 for all interfaces, 127.0.0.1 for localhost only)
BMP_LISTEN_HOST=0.0.0.0

# TCP port for BMP connections (standard BMP port is 11019)
BMP_LISTEN_PORT=11019

# ===================================================================
# Logging Configuration
# ===================================================================

# Log level: DEBUG, INFO, WARNING, ERROR
# Note: DEBUG logs include hex dumps and significantly increases log volume
LOG_LEVEL=INFO

# ===================================================================
# Database Configuration
# ===================================================================

# PostgreSQL/TimescaleDB connection parameters
DB_HOST=localhost
DB_PORT=5432
DB_USER=bmpmon
DB_PASSWORD=changeme
DB_NAME=bmpmon

# Database Connection Pool Settings
# Minimum number of connections to maintain
DB_POOL_MIN_SIZE=5

# Maximum number of connections in pool
DB_POOL_MAX_SIZE=10

# ===================================================================
# Sentry Integration (Optional)
# ===================================================================

# Sentry DSN for error tracking
# Leave empty to disable Sentry integration
# Example: https://examplePublicKey@o0.ingest.sentry.io/0
SENTRY_DSN=

# Sentry environment name (e.g., development, staging, production)
SENTRY_ENVIRONMENT=development

# Percentage of transactions to trace (0.0 to 1.0)
# Use 1.0 in development, 0.1 in production to reduce overhead
SENTRY_TRACES_SAMPLE_RATE=0.1
